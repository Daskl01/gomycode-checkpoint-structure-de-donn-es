ALGORITHM problème_2
VAR
    htab1:HASH_TABLE<integer,integer>
    htab2:HASH_TABLE<integer,integer>
    tab1:ARRAY_OF_INTEGER
    tab2:ARRAY_OF_INTEGER
    sum:INTEGER:=0
BEGIN
    FOR i FROM 0 TO M-1 STEP 1  DO
        FOR i FROM 0 TO N-1 STEP 1  DO
           IF (htable.get[i]=htable.get[j]) THEN
               sum:=sum+tab1[i]+tab2[j]
           END_IF
           
        END_FOR
    END_FOR
        write("la somme est :"sum)   
END